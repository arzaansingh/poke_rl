# ─── Binary models (large Q-table pickles — regenerable from training) ───
*.pkl
*.pkl.tmp

# ─── Python cache ───
__pycache__/
*.pyc
*.pyo

# ─── Runtime-only logs (verbose, replaceable, not analysis data) ───
# CSVs, JSON configs, and plots are KEPT — those are the research artifacts.
*.log
train_stdout.log
live_status*.json
showdown_logs/
stdout_logs/

# ─── OS + IDE ───
.DS_Store
.vscode/
.idea/

# ─── External dependencies / separate projects ───
pokemon-showdown/

# ─── Pre-research iterative code (not part of the V1-V6 research line) ───
v13_hq_dense_g1/
v15/
v16/
v16gen4/
v17/
v17.1gen4/
v17gen4/
v18gen4/
backup/

# ─── Personal/misc ───
Terminal Saved Output.txt
dpp_ou_teams.txt
.claude/

# ─── Node / React build artifacts (poster-visuals React app) ───
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# ─── LaTeX build artifacts (keep .tex + figures, drop compile byproducts) ───
*.aux
*.fdb_latexmk
*.fls
*.synctex.gz
*.toc
*.out
