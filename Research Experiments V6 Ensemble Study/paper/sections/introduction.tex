% Section 1: Introduction (~1 page, ~700 words)
% Write THIRD, after Method and Results are stable.
%
% Scope: this paper is about V6 (the K=10 tabular Q-function ensemble on
% Pokemon gen4ou) only. Do NOT claim deep-RL extensions in the introduction:
% they have not been implemented or evaluated. Reference V5 only as the
% prior factorial study whose winning configuration becomes the per-member
% training pipeline for V6; details of V5 live in Appendix A.

\section{Introduction}
\label{sec:introduction}

\emph{[Prose to be written after Friday's meeting once the contribution framing is locked in.]}

% Suggested structure (from OUTLINE.md):
%
% Paragraph 1 — motivation. Tabular RL ceiling effects in stochastic
% partially-observable two-player games; Pokemon as a 10^12-state
% benchmark.
%
% Paragraph 2 — why ensembling at all. Brief setup of the question.
% Cite Wiering & van Hasselt 2008, Osband 2016, Lan 2020.
%
% Paragraph 3 — what is new in this paper specifically:
%   (a) a read-only inference rule for tabular Q-function ensembles with
%       heuristic-prior fallback for unseen states;
%   (b) the first head-to-head comparison of soft / hard /
%       confidence-weighted voting in tabular Q-learning at K up to 10,
%       on a non-toy stochastic two-player game;
%   (c) a compute-matched single-agent control (5M battles, single agent)
%       against which the ensemble is benchmarked at the same evaluation
%       budget.
%
% Paragraph 4 — result preview.
% K=10 ensemble = 66.8 +/- 0.3 % vs SimpleHeuristicsPlayer over 100k
% battles, +12.6 pp over the compute-matched single-agent baseline.
%
% Paragraph 5 — what this paper does NOT do. Inherited components
% (hierarchical decomposition, heuristic Q-initialization) come from
% prior factorial study (Appendix A); we do not claim them as
% contributions. Deep-RL ensemble variants are open questions and not
% addressed here.
