% Abstract — write last; ~200 words; structure follows OUTLINE.md.
%
% 5-sentence skeleton:
% 1. Setup — single-agent tabular RL ceiling effect in stochastic POMDPs.
% 2. Method — K independent Q(lambda) agents combined by voting at inference, read-only.
% 3. Headline result — K=10 ensemble 66.8% vs heuristic, +12.6pp over compute-matched single agent.
% 4. K-saturation + voting comparison — Osband-shape curve, voting rule choice has small effect.
% 5. Significance — first Q-function ensemble study on Pokemon; methodology generalizes.

TODO write abstract last.
